# INMO enlaces PDF (esqueleto)

Prototipo con backend Express + TypeScript y frontend Vite + React. Incluye scraper modular (Playwright + Cheerio), generación de PDF con PDFKit, rutas públicas con expiración y esquema de base de datos en Prisma.
